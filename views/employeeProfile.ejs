<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
        <!-- Header -->
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-2xl font-bold text-gray-800">Employee Profile</h1>
            <a href="/dashboard/hr/employees/list" class="flex items-center text-blue-600 hover:text-blue-800 transition-colors">
                <i class="fas fa-arrow-left mr-2"></i> Back to Employee List
            </a>
        </div>

        <!-- Profile Card -->
        <div class="bg-white rounded-xl shadow-md overflow-hidden">
            <!-- Profile Header -->
            <div class="bg-gradient-to-r from-blue-500 to-indigo-600 p-6 text-white">
                <div class="flex flex-col md:flex-row items-center">
                    <div class="mb-4 md:mb-0 md:mr-6">
                        <% if (employee.photo) { %>
                            <img src="<%= employee.photo %>" alt="Profile Photo" class="w-32 h-32 rounded-full border-4 border-white shadow-lg object-cover">
                        <% } else { %>
                            <div class="w-32 h-32 rounded-full border-4 border-white bg-blue-300 shadow-lg flex items-center justify-center">
                                <i class="fas fa-user text-4xl text-white"></i>
                            </div>
                        <% } %>
                    </div>
                    <div class="text-center md:text-left">
                        <h2 class="text-2xl font-bold"><%= employee.name %></h2>
                        <p class="mt-1 opacity-90"><%= employee.designation %> | <%= employee.role %></p>
                        <div class="mt-3 flex flex-wrap justify-center md:justify-start gap-2">
                            <span class="bg-white bg-opacity-20 px-3 py-1 rounded-full text-sm">ID: <%= employee.emp_id %></span>
                            <span class="bg-white bg-opacity-20 px-3 py-1 rounded-full text-sm"><%= employee.assign_city %></span>
                            <% if (employee.leave_balance) { %>
                                <span class="bg-white bg-opacity-20 px-3 py-1 rounded-full text-sm"><%= employee.leave_balance %> Leaves</span>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Profile Details -->
            <div class="p-6">
                <!-- Personal Information -->
                <div class="mb-8">
                    <h3 class="text-xl font-semibold text-gray-800 mb-4 pb-2 border-b border-gray-200 flex items-center">
                        <i class="fas fa-user-circle mr-2 text-blue-500"></i> Personal Information
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Employee ID</p>
                            <p class="font-medium"><%= employee.emp_id %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Email</p>
                            <p class="font-medium"><%= employee.email %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">password</p>
                            <p class="font-medium"><%= employee.pin %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Date of Birth</p>
                            <p class="font-medium"><%= employee.dob ? new Date(employee.dob).toLocaleDateString('en-GB') : "N/A" %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">City</p>
                            <p class="font-medium"><%= employee.assign_city %></p>
                        </div>
                    </div>
                </div>

                <!-- Job Information -->
                <div class="mb-8">
                    <h3 class="text-xl font-semibold text-gray-800 mb-4 pb-2 border-b border-gray-200 flex items-center">
                        <i class="fas fa-briefcase mr-2 text-blue-500"></i> Job Information
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Joining Date</p>
                            <p class="font-medium"><%= employee.joining_date ? new Date(employee.joining_date).toLocaleDateString('en-GB') : "N/A" %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Resign Date</p>
                            <p class="font-medium"><%= employee.resign_date ? new Date(employee.resign_date).toLocaleDateString('en-GB') : "N/A" %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Employment Status</p>
                            <p class="font-medium capitalize"><%= employee.employment_status || "N/A" %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Leave Balance</p>
                            <p class="font-medium"><%= employee.leave_balance %></p>
                        </div>
                    </div>
                </div>

                <!-- Bank Information -->
                <div class="mb-8">
                    <h3 class="text-xl font-semibold text-gray-800 mb-4 pb-2 border-b border-gray-200 flex items-center">
                        <i class="fas fa-university mr-2 text-blue-500"></i> Bank Information
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">Bank Account Number</p>
                            <p class="font-medium"><%= employee.bank_number %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-lg">
                            <p class="text-sm text-gray-500">IFSC Code</p>
                            <p class="font-medium"><%= employee.ifsc %></p>
                        </div>
                    </div>
                </div>

                <!-- Documents -->
                <div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-4 pb-2 border-b border-gray-200 flex items-center">
                        <i class="fas fa-file-alt mr-2 text-blue-500"></i> Documents
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div class="bg-gray-50 p-4 rounded-lg flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500">Passbook</p>
                                <p class="font-medium">
                                    <% if (employee.passbook_image) { %>
                                        <a href="<%= employee.passbook_image %>" target="_blank" class="text-blue-600 hover:text-blue-800 transition-colors">View Document</a>
                                    <% } else { %> N/A <% } %>
                                </p>
                            </div>
                            <i class="fas fa-file-pdf text-2xl text-red-500"></i>
                        </div>
                        
                        <div class="bg-gray-50 p-4 rounded-lg flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500">PAN Card</p>
                                <p class="font-medium">
                                    <% if (employee.pan_card) { %>
                                        <a href="<%= employee.pan_card %>" target="_blank" class="text-blue-600 hover:text-blue-800 transition-colors">View Document</a>
                                    <% } else { %> N/A <% } %>
                                </p>
                            </div>
                            <i class="fas fa-file-pdf text-2xl text-red-500"></i>
                        </div>
                        
                        <div class="bg-gray-50 p-4 rounded-lg flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500">Aadhar Card</p>
                                <p class="font-medium">
                                    <% if (employee.aadhar_card) { %>
                                        <a href="<%= employee.aadhar_card %>" target="_blank" class="text-blue-600 hover:text-blue-800 transition-colors">View Document</a>
                                    <% } else { %> N/A <% } %>
                                </p>
                            </div>
                            <i class="fas fa-file-pdf text-2xl text-red-500"></i>
                        </div>
                        
                        <div class="bg-gray-50 p-4 rounded-lg flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500">Offer Letter</p>
                                <p class="font-medium">
                                    <% if (employee.offer_letter) { %>
                                        <a href="<%= employee.offer_letter %>" target="_blank" class="text-blue-600 hover:text-blue-800 transition-colors">View Document</a>
                                    <% } else { %> N/A <% } %>
                                </p>
                            </div>
                            <i class="fas fa-file-pdf text-2xl text-red-500"></i>
                        </div>
                        
                        <div class="bg-gray-50 p-4 rounded-lg flex justify-between items-center">
                            <div>
                                <p class="text-sm text-gray-500">Experience Letter</p>
                                <p class="font-medium">
                                    <% if (employee.last_company_experience_letter) { %>
                                        <a href="<%= employee.last_company_experience_letter %>" target="_blank" class="text-blue-600 hover:text-blue-800 transition-colors">View Document</a>
                                    <% } else { %> N/A <% } %>
                                </p>
                            </div>
                            <i class="fas fa-file-pdf text-2xl text-red-500"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
